[Server]
AppMode = "debug"   # debug 模式日志显示在控制台, release 模式日志输出到文件
BackPort = ":8765"  # 前台服务端口
FrontPort = ":5678" # 后台服务端口

[JWT]
SigningKey = "abc123321"
ExpiresTime = 24         # 过期时间 (小时)
Issuer = "ginblog"

[Mysql]
Path = "47.103.138.81"
# Path: "127.0.0.1"
Port = "3306"
Config = ""                 # 其他配置, 例如时区
Dbname = "ginblog"
Username = "root"
Password = "Mingjiekeji426"
# Password = "1234"
MaxIdleConns = 10  # 连接池相关
MaxOpenConns = 100 # 连接池相关
LogMode = "error"  # 控制台打印日志级别 "silent"、"error"、"warn"、"info" 不填默认 info  填入silent可以关闭控制台日志

[Redis]
DB = 7
Addr = "47.103.138.81:6379"
Password = "123456"
# Addr = '127.0.0.1:6379'
# Password = ''

[Session]
Name = "mysession"
Salt = "salt"
MaxAge = 600       # 秒

[Upload]
# Size = 32 # 设置上传文件的最大值 (单位 M)
OssType = "qiniu"              # 控制存储: local、qiniu
Path = "public/uploaded/"      # 文件访问路径
StorePath = "public/uploaded/" # 文件上传路径

[Zap]
Level = "debug"        # 日志级别
Format = "console"     # 输出格式
Prefix = "[GIN-BLOG] " # 日志前缀
Director = "log"       # 存放日志的文件夹
ShowLine = false       # 显示哪行调用
LogInConsole = true    # 输出控制台

[Qiniu]
ImgPath = "http://rjh34xerw.hn-bkt.clouddn.com"        # 外链
Zone = "ZoneHuaDong"
Bucket = "yusael-ginblog"
AccessKey = "EB0heiSomzwk7-Em_5M5PzY-5w1xvD1XOMvMdblW"
SecretKey = "OjXt9g4XGZUKEKiaEfTOvgYdCXbPZFspdSIULxUY"
UseHttps = false                                       # 是否使用 https 加速
UseCdnDomains = false                                  # 上传是否使用 CDN 上传加速
