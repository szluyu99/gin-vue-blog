// https://www.typescriptlang.org/tsconfig/
// 存在 jsconfig.json 文件表示该目录是 JavaScript 项目的根目录, 用于指定 JavaScript 语言服务提供的功能选项
{
  "compilerOptions": {
    "target": "ES6",
    "baseUrl": "./",
    "paths": {
      // 指定别名, 智能感知路径
      "~/*": ["./*"],
      "@/*": ["./src/*"]
    },
    "jsx": "preserve",
    "types": ["vue/ref-macros"]
  },
  "exclude": ["node_modules", "dist"]
}
